# Contributing

Use feature branches, keep pull requests focused, and run the frontend build and backend test suite before opening a pull request. Never commit secrets; update `.env.example` when a configuration key changes.
