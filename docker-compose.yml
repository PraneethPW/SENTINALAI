services:
  api:
    build: ./backend
    env_file: .env
    ports: ["8000:8000"]
  web:
    build: ./frontend
    environment:
      VITE_API_URL: ${VITE_API_URL:-http://localhost:8000/api/v1}
    ports: ["5173:80"]
    depends_on: [api]
